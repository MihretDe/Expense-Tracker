/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Enable dark mode (must be before theme/plugins)
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}